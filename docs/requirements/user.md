# User

- The system allows the users to log in with pre-defined username and password
- The users can see all tournaments but only manage their own data (frontend and API)
- Non-logged in visitors can only read data (frontend and API)